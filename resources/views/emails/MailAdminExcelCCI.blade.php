<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{{ $subject ?? 'CCI Excel Export' }}</title>
</head>
<body>
    <h2>{{ $subject ?? 'CCI Excel Export' }}</h2>
    <p>{{ $msg }}</p>
</body>
</html>