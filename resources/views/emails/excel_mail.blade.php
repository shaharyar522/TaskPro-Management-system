<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{{ $subject ?? 'Frontier Excel Export' }}</title>
</head>
<body>
    <h2>{{ $subject ?? 'Frontier Excel Export' }}</h2>
    <p>{{ $msg }}</p>
</body>
</html>
